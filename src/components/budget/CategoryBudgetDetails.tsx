import { FC } from 'react';
import styled from 'styled-components';

interface CategoryBudgetDetailsProps {}

export const CategoryBudgetDetails: FC<CategoryBudgetDetailsProps> = ({}) => {
  return <Styled.container>CategoryBudgetDetails</Styled.container>;
};

const Styled = {
  container: styled.div``,
};
